name: Test rai-audit-ml

on:
  push:
    paths:
      - "packages/rai-audit-ml/**"
      - "packages/rai-audit-core/**"
      - ".github/workflows/test-ml.yml"
  pull_request:
    paths:
      - "packages/rai-audit-ml/**"
      - "packages/rai-audit-core/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          python-version: "3.12"
      - run: uv pip install -e "packages/rai-audit-core" -e "packages/rai-audit-ml" pytest
      - run: uv run pytest packages/rai-audit-ml/tests -v
