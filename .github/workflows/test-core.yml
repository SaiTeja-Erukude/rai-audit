name: Test rai-audit-core

on:
  push:
    paths:
      - "packages/rai-audit-core/**"
      - ".github/workflows/test-core.yml"
  pull_request:
    paths:
      - "packages/rai-audit-core/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          python-version: "3.12"
      - run: uv venv
      - run: uv pip install -e "packages/rai-audit-core" pytest
      - run: uv run pytest packages/rai-audit-core/tests -v
